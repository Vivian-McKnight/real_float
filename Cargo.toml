[package]
name = "algebraic_float"
version = "0.1.0"
edition = "2024"

[dependencies]
serde = { version = "1.0.229", default-features = false, features = ["serde_derive"], optional = true }

[features]
serde = ["dep:serde"]
